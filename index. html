<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <meta name="description" content="NIW Evidence-Based Case Structuring Framework — 자격이 아니라 논리 구조와 증거 매핑으로 접근합니다." />
  <title>NIW Readiness Analysis · Evidence Mapping Framework</title>

  <style>
    :root{
      --bg:#0b1020;
      --bg2:#0e1630;
      --card:rgba(255,255,255,.06);
      --card2:rgba(255,255,255,.08);
      --border:rgba(255,255,255,.12);
      --text:rgba(255,255,255,.92);
      --muted:rgba(255,255,255,.70);
      --muted2:rgba(255,255,255,.60);
      --accent:#7aa6ff;   /* cobalt-ish */
      --accent2:#f2c66d;  /* gold-ish */
      --danger:#ff7a7a;
      --ok:#7affc3;
      --shadow: 0 18px 60px rgba(0,0,0,.40);
      --radius: 18px;
      --radius2: 26px;
      --max: 1100px;
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Apple SD Gothic Neo, Noto Sans KR, Helvetica, Arial, "Apple Color Emoji","Segoe UI Emoji";
      background:
        radial-gradient(900px 500px at 15% 10%, rgba(122,166,255,.22), transparent 60%),
        radial-gradient(900px 500px at 85% 20%, rgba(242,198,109,.18), transparent 60%),
        radial-gradient(900px 650px at 50% 90%, rgba(122,255,195,.10), transparent 60%),
        linear-gradient(180deg, var(--bg), var(--bg2));
      color: var(--text);
      line-height: 1.55;
    }
    a{color:inherit}
    .wrap{max-width:var(--max); margin:0 auto; padding:0 20px;}
    .nav{
      position:sticky; top:0; z-index:30;
      backdrop-filter: blur(14px);
      background: rgba(10,16,32,.55);
      border-bottom: 1px solid var(--border);
    }
    .nav-inner{
      display:flex; align-items:center; justify-content:space-between;
      padding:14px 0;
      gap:14px;
    }
    .brand{
      display:flex; align-items:center; gap:10px;
      font-weight:700; letter-spacing:.2px;
    }
    .logo{
      width:34px; height:34px; border-radius:10px;
      background: linear-gradient(135deg, rgba(122,166,255,.95), rgba(242,198,109,.95));
      box-shadow: 0 10px 25px rgba(0,0,0,.35);
    }
    .brand small{display:block; font-weight:600; color:var(--muted); letter-spacing:.2px}
    .nav-actions{display:flex; align-items:center; gap:10px; flex-wrap:wrap; justify-content:flex-end;}
    .btn{
      display:inline-flex; align-items:center; justify-content:center; gap:10px;
      padding:11px 14px;
      border-radius: 999px;
      border:1px solid var(--border);
      background: rgba(255,255,255,.05);
      color: var(--text);
      text-decoration:none;
      font-weight:650;
      cursor:pointer;
      transition: transform .14s ease, background .14s ease, border-color .14s ease;
      user-select:none;
    }
    .btn:hover{transform: translateY(-1px); background: rgba(255,255,255,.07); border-color: rgba(255,255,255,.20);}
    .btn.primary{
      border-color: rgba(122,166,255,.55);
      background: linear-gradient(135deg, rgba(122,166,255,.25), rgba(242,198,109,.15));
      box-shadow: 0 16px 45px rgba(0,0,0,.35);
    }
    .btn.primary:hover{border-color: rgba(122,166,255,.70)}
    .btn.gold{
      border-color: rgba(242,198,109,.55);
      background: linear-gradient(135deg, rgba(242,198,109,.22), rgba(122,166,255,.10));
    }
    .pill{
      display:inline-flex; align-items:center; gap:8px;
      padding:8px 12px;
      border-radius: 999px;
      background: rgba(255,255,255,.06);
      border: 1px solid var(--border);
      color: var(--muted);
      font-weight:650;
      font-size: 13px;
      letter-spacing:.2px;
    }
    .hero{
      padding: 52px 0 24px;
    }
    .hero-grid{
      display:grid;
      grid-template-columns: 1.2fr .8fr;
      gap: 18px;
      align-items:stretch;
    }
    @media (max-width: 900px){
      .hero-grid{grid-template-columns: 1fr; gap: 14px;}
      .nav-inner{padding:12px 0}
    }
    .card{
      border: 1px solid var(--border);
      background: var(--card);
      border-radius: var(--radius2);
      box-shadow: var(--shadow);
    }
    .hero-card{
      padding: 26px;
      overflow:hidden;
      position:relative;
    }
    .hero-card:before{
      content:"";
      position:absolute; inset:-2px;
      background: radial-gradient(550px 220px at 22% 15%, rgba(122,166,255,.18), transparent 60%),
                  radial-gradient(550px 220px at 82% 20%, rgba(242,198,109,.12), transparent 60%);
      pointer-events:none;
    }
    .hero-content{position:relative}
    h1{
      margin: 10px 0 10px;
      font-size: clamp(28px, 3.6vw, 44px);
      line-height: 1.12;
      letter-spacing: -0.6px;
    }
    .lead{
      margin: 0 0 18px;
      color: var(--muted);
      font-size: 16px;
      max-width: 62ch;
    }
    .hero-actions{display:flex; gap:10px; flex-wrap:wrap; margin-top: 14px;}
    .kpi{
      padding: 18px 18px 16px;
      border-radius: var(--radius2);
      border: 1px solid var(--border);
      background: rgba(255,255,255,.05);
    }
    .kpi h3{
      margin:0 0 6px;
      font-size: 14px; color: var(--muted2); font-weight:700; letter-spacing:.2px;
      text-transform: uppercase;
    }
    .kpi .big{font-size: 20px; font-weight:800; letter-spacing:-.2px}
    .kpi .sub{color: var(--muted); font-size: 13px; margin-top: 6px}
    .side{
      padding: 22px;
      display:flex;
      flex-direction:column;
      gap: 12px;
    }
    .side h2{
      margin:0;
      font-size: 16px;
      letter-spacing:-.2px;
    }
    .side p{margin:0; color: var(--muted); font-size: 14px}
    .list{
      display:grid; gap:10px; margin-top: 6px;
    }
    .li{
      display:flex; gap:10px;
      padding: 12px;
      border-radius: 14px;
      border: 1px solid var(--border);
      background: rgba(255,255,255,.05);
    }
    .dot{
      width: 10px; height: 10px; border-radius: 999px;
      margin-top: 6px;
      background: rgba(122,166,255,.75);
      box-shadow: 0 0 0 6px rgba(122,166,255,.12);
      flex: 0 0 auto;
    }
    .dot.gold{background: rgba(242,198,109,.85); box-shadow: 0 0 0 6px rgba(242,198,109,.12);}
    .dot.ok{background: rgba(122,255,195,.90); box-shadow: 0 0 0 6px rgba(122,255,195,.12);}
    section{padding: 28px 0;}
    .section-head{
      display:flex; align-items:flex-end; justify-content:space-between; gap: 14px;
      margin-bottom: 14px;
    }
    .section-head h2{
      margin:0;
      font-size: 22px;
      letter-spacing: -0.3px;
    }
    .section-head p{margin:0; color: var(--muted); max-width: 70ch}
    .grid-3{display:grid; gap: 12px; grid-template-columns: repeat(3, 1fr);}
    @media (max-width: 900px){ .grid-3{grid-template-columns: 1fr;} }
    .mini{
      padding: 18px;
      border-radius: var(--radius);
      border: 1px solid var(--border);
      background: rgba(255,255,255,.05);
    }
    .mini h3{
      margin:0 0 8px;
      font-size: 15px;
      letter-spacing:-.2px;
    }
    .mini p{margin:0; color: var(--muted); font-size: 14px}
    .badge{
      display:inline-flex; align-items:center; gap:8px;
      font-size: 12px; font-weight:750;
      color: rgba(255,255,255,.82);
      background: rgba(122,166,255,.12);
      border: 1px solid rgba(122,166,255,.30);
      padding: 6px 10px;
      border-radius: 999px;
      margin-bottom: 10px;
    }
    .badge.gold{background: rgba(242,198,109,.10); border-color: rgba(242,198,109,.30);}
    .badge.muted{background: rgba(255,255,255,.06); border-color: var(--border); color: var(--muted);}
    .sample{
      padding: 22px;
      border-radius: var(--radius2);
      border: 1px solid var(--border);
      background: rgba(255,255,255,.05);
    }
    .q{
      border-radius: 16px;
      border: 1px solid var(--border);
      background: rgba(0,0,0,.18);
      padding: 16px;
      margin-top: 12px;
    }
    .q h4{margin:0 0 8px; font-size: 15px; letter-spacing:-.2px}
    .q p{margin:0 0 10px; color: var(--muted); font-size: 14px}
    .choices{display:grid; gap:8px}
    .choice{
      display:flex; align-items:flex-start; gap:10px;
      padding: 10px 12px;
      border-radius: 14px;
      border: 1px solid var(--border);
      background: rgba(255,255,255,.05);
      cursor:pointer;
      transition: transform .12s ease, background .12s ease;
    }
    .choice:hover{transform: translateY(-1px); background: rgba(255,255,255,.07)}
    input[type="radio"], input[type="checkbox"]{
      margin-top: 2px;
      accent-color: var(--accent);
      transform: scale(1.05);
    }
    .evidence{
      display:flex; flex-wrap:wrap; gap:8px;
      margin-top: 10px;
    }
    .chip{
      display:inline-flex; align-items:center; gap:8px;
      padding: 7px 10px;
      border-radius: 999px;
      border: 1px solid var(--border);
      background: rgba(255,255,255,.05);
      color: var(--muted);
      font-size: 12px;
      font-weight: 650;
    }
    .chip strong{color: rgba(255,255,255,.90)}
    .divider{
      height:1px; background: rgba(255,255,255,.12);
      margin: 14px 0;
    }
    .result{
      padding: 22px;
      border-radius: var(--radius2);
      border: 1px solid rgba(122,166,255,.35);
      background: linear-gradient(180deg, rgba(122,166,255,.10), rgba(255,255,255,.03));
    }
    .result h3{margin:0 0 10px; font-size: 18px; letter-spacing:-.2px}
    .result p{margin:0; color: var(--muted); max-width: 80ch}
    .cta{
      padding: 26px;
      border-radius: var(--radius2);
      border: 1px solid rgba(242,198,109,.32);
      background: radial-gradient(800px 220px at 20% 0%, rgba(242,198,109,.14), transparent 60%),
                  radial-gradient(800px 220px at 80% 0%, rgba(122,166,255,.12), transparent 60%),
                  rgba(255,255,255,.04);
      box-shadow: var(--shadow);
    }
    .cta-grid{
      display:grid;
      grid-template-columns: 1.1fr .9fr;
      gap: 14px;
      align-items:stretch;
    }
    @media (max-width: 900px){ .cta-grid{grid-template-columns: 1fr;} }
    .cta h2{margin:0 0 10px; font-size: 22px; letter-spacing:-.3px}
    .cta p{margin:0; color: var(--muted); max-width: 70ch}
    .form{
      border-radius: 18px;
      border: 1px solid var(--border);
      background: rgba(0,0,0,.18);
      padding: 14px;
    }
    .row{display:grid; grid-template-columns: 1fr 1fr; gap: 10px;}
    @media (max-width: 700px){ .row{grid-template-columns: 1fr;} }
    label{display:block; font-size: 12px; font-weight: 750; color: var(--muted2); margin: 10px 0 6px;}
    input[type="text"], input[type="email"], textarea, select{
      width:100%;
      padding: 11px 12px;
      border-radius: 14px;
      border: 1px solid rgba(255,255,255,.14);
      background: rgba(255,255,255,.06);
      color: var(--text);
      outline:none;
    }
    textarea{min-height: 92px; resize: vertical;}
    .form small{display:block; margin-top:10px; color: var(--muted2); font-size: 12px; line-height: 1.45;}
    .foot{
      padding: 24px 0 40px;
      color: var(--muted2);
      font-size: 12px;
      border-top: 1px solid rgba(255,255,255,.10);
      margin-top: 24px;
    }
    .foot a{color: rgba(255,255,255,.80)}
    .two-col{display:grid; grid-template-columns: 1fr 1fr; gap:12px;}
    @media (max-width: 900px){ .two-col{grid-template-columns: 1fr;} }
    .callout{
      border-radius: 16px;
      border: 1px solid rgba(255,122,122,.30);
      background: rgba(255,122,122,.06);
      padding: 14px;
      color: rgba(255,255,255,.88);
    }
    .callout strong{color: rgba(255,255,255,.95)}
    .muted{color: var(--muted)}
    .hidden{display:none}
    .anchor{scroll-margin-top: 84px;}
  </style>
</head>

<body>
  <!-- Top Nav -->
  <div class="nav">
    <div class="wrap">
      <div class="nav-inner">
        <div class="brand" aria-label="Brand">
          <div class="logo" aria-hidden="true"></div>
          <div>
            NIW Evidence Mapping
            <small>Case Structuring Framework</small>
          </div>
        </div>

        <div class="nav-actions">
          <a class="btn" href="#how" title="What this does">어떻게 작동하나요</a>
          <a class="btn gold" href="#sample" title="Try sample">샘플 질문 보기</a>
          <a class="btn primary" href="#start" title="Start">🔍 구조 점검 시작</a>
        </div>
      </div>
    </div>
  </div>

  <!-- HERO -->
  <header class="hero">
    <div class="wrap">
      <div class="hero-grid">
        <div class="card hero-card">
          <div class="hero-content">
            <span class="pill">5–7분 · 점수 즉시공개 없음 · 증거 중심</span>
            <h1>NIW는 ‘자격’의 문제가 아니라<br/>‘논리 구조’의 문제입니다.</h1>
            <p class="lead">
              많은 NIW 청원은 스펙이 부족해서가 아니라,
              <strong>이민국이 이해할 수 없는 구조</strong>로 제출되어 중간에서 멈춥니다.
              이 페이지는 변호사가 실제로 케이스를 볼 때 가장 먼저 하는
              <strong>사고 순서(Framework)</strong>와 <strong>증거 매핑 방식</strong>을 그대로 보여드립니다.
            </p>

            <div class="hero-actions">
              <a class="btn primary" href="#start">🔍 내 케이스 구조 점검해 보기</a>
              <a class="btn" href="#how">프레임 설명 보기</a>
            </div>

            <div class="divider"></div>

            <div class="two-col">
              <div class="kpi">
                <h3>이 도구의 관점</h3>
                <div class="big">주장(말)보다 증거(문서)</div>
                <div class="sub">“가능성”을 말하기 전에, “제출 가능한 자료”가 정렬되어 있는지부터 봅니다.</div>
              </div>
              <div class="kpi">
                <h3>핵심 질문</h3>
                <div class="big">연속성 · 구체성 · 공공성</div>
                <div class="sub">계획이 아니라, 이미 해온 전문 활동이 미국에서 이어지는지 확인합니다.</div>
              </div>
            </div>

          </div>
        </div>

        <aside class="card side" aria-label="Highlights">
          <h2>이 페이지에서 고객이 ‘감동’하는 지점</h2>
          <p>“점수표”가 아니라, <strong>전문가의 사고 구조</strong>를 체험하도록 설계되어 있습니다.</p>

          <div class="list">
            <div class="li">
              <div class="dot"></div>
              <div>
                <strong>사고 순서를 공개합니다</strong><br/>
                <span class="muted">NIW 3 Prongs를 ‘서류’가 아니라 ‘논증’으로 정렬합니다.</span>
              </div>
            </div>
            <div class="li">
              <div class="dot gold"></div>
              <div>
                <strong>주장 대신 증거를 요구합니다</strong><br/>
                <span class="muted">“있다”가 아니라 “제출 가능한 형태로 있다”가 기준입니다.</span>
              </div>
            </div>
            <div class="li">
              <div class="dot ok"></div>
              <div>
                <strong>약점을 숨기지 않습니다</strong><br/>
                <span class="muted">강점보다, 논리가 끊기는 지점을 먼저 드러냅니다.</span>
              </div>
            </div>
          </div>

          <div class="callout">
            <strong>중요:</strong> 이 도구는 “합격/불합격”을 판단하지 않습니다.  
            대신, <strong>현재 자료가 NIW 논리 흐름에 얼마나 정렬되어 있는지</strong>를 진단합니다.
          </div>
        </aside>
      </div>
    </div>
  </header>

  <!-- WHY -->
  <section class="anchor" id="how">
    <div class="wrap">
      <div class="section-head">
        <div>
          <h2>왜 “스펙이 좋은데도” NIW가 거절될까요?</h2>
          <p>
            이민국은 논문 개수나 직함을 단순 합산하지 않습니다.
            결국 핵심은 단 하나입니다: <strong>미국에서 하려는 일이 ‘이미 해오던 전문 활동’의 연장인가</strong>.
          </p>
        </div>
        <span class="badge muted">Framework-first</span>
      </div>

      <div class="grid-3">
        <div class="mini">
          <span class="badge">① 연속성</span>
          <h3>계획(희망) 중심이면 논리가 출발하지 않습니다</h3>
          <p>“미국에서 이런 일을 하고 싶다”는 자연스럽지만, NIW의 ‘Specific Endeavor’로는 부족합니다. 과거 활동과의 연속성을 문서로 보여야 합니다.</p>
        </div>
        <div class="mini">
          <span class="badge gold">② 증거 매핑</span>
          <h3>주장과 증거가 1:1로 연결되어야 합니다</h3>
          <p>“중요하다”는 의견이 아니라 자료로만 인정됩니다. 정책·시장·산업 맥락과 개인 실적이 연결되어야 설득력이 생깁니다.</p>
        </div>
        <div class="mini">
          <span class="badge muted">③ 배열</span>
          <h3>NIW는 ‘설명’이 아니라 ‘배치’입니다</h3>
          <p>좋은 실적이 있어도, 논리 흐름이 끊기면 국가적 중요성 단계까지 가보지 못합니다. 이 도구는 그 끊김을 먼저 찾습니다.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- SAMPLE QUESTIONS -->
  <section class="anchor" id="sample">
    <div class="wrap">
      <div class="section-head">
        <div>
          <h2>샘플 질문 · 고객이 “생각하게 되는” 구조</h2>
          <p>
            점수보다 먼저, <strong>변호사가 실제로 던지는 질문</strong>을 체험하게 합니다.
            답을 쓰는 순간, 고객은 “이게 그냥 테스트가 아니구나”를 느낍니다.
          </p>
        </div>
        <span class="badge gold">Evidence-required</span>
      </div>

      <div class="sample card">
        <div class="q">
          <h4>Q1. 미국에서도 그대로 이어질 수 있는 ‘하나의 전문 축’은 무엇입니까?</h4>
          <p>이 질문은 “정답”을 요구하지 않습니다. 다만 이 답이 없으면 NIW 논리는 출발하지 않습니다.</p>
          <div class="choices" role="radiogroup" aria-label="Q1 choices">
            <label class="choice"><input type="radio" name="q1" /> 아직 정리되지 않았습니다</label>
            <label class="choice"><input type="radio" name="q1" /> 여러 축이 있고, 하나로 좁히는 중입니다</label>
            <label class="choice"><input type="radio" name="q1" /> 명확한 하나의 축이 있습니다</label>
          </div>

          <div class="evidence" aria-label="Evidence hints">
            <span class="chip">필요 증거: <strong>프로젝트 산출물</strong></span>
            <span class="chip">필요 증거: <strong>업무/연구 기록</strong></span>
            <span class="chip">필요 증거: <strong>타임라인</strong></span>
          </div>
        </div>

        <div class="q">
          <h4>Q2. 위 활동이 ‘과거에 반복 수행’되었음을 증명할 수 있습니까?</h4>
          <p>“있다”가 아니라 “제출 가능한 형태로 있다”가 기준입니다.</p>
          <div class="choices" role="group" aria-label="Q2 evidence checklist">
            <label class="choice"><input type="checkbox" /> 반복 수행을 보여주는 프로젝트/논문/보고서</label>
            <label class="choice"><input type="checkbox" /> 외부(독립) 평가: 인용/심사/초청/평가서 등</label>
            <label class="choice"><input type="checkbox" /> 내 역할이 ‘핵심 주도’였음을 보여주는 문서</label>
            <label class="choice"><input type="checkbox" /> 미국 맥락 연결: 시장/정책/산업 자료(보고서/통계 등)</label>
          </div>

          <div class="evidence">
            <span class="chip">주의: <strong>에세이 단독</strong>은 점수 상한이 낮습니다</span>
            <span class="chip">권장: <strong>독립 자료 + 실물 증거</strong></span>
          </div>
        </div>

        <div class="result" id="resultBox" aria-live="polite">
          <h3>이 샘플은 “합격/불합격”을 말하지 않습니다.</h3>
          <p>
            대신, 고객이 스스로 깨닫게 만듭니다:  
            <strong>“내가 가진 자료가 NIW 논리 흐름에 맞게 정렬되어 있는가?”</strong>  
            실제 상담에서는 이 정렬 결과를 기반으로, 보강해야 할 증거 묶음을 설계합니다.
          </p>
        </div>

        <div class="divider"></div>

        <div class="muted" style="font-size:13px;">
          * 본 샘플은 UI 데모입니다. 실제 서비스에서는 질문 수/분기 로직/증거 업로드 단계/결과 문안이 더 정교하게 구성됩니다.
        </div>
      </div>
    </div>
  </section>

  <!-- WHAT IT DOES NOT DO -->
  <section class="anchor" id="limits">
    <div class="wrap">
      <div class="section-head">
        <div>
          <h2>이 도구가 하지 않는 것</h2>
          <p>
            고객 신뢰를 만드는 핵심 섹션입니다. “과장”을 버리고, 대신 “구조”로 승부합니다.
          </p>
        </div>
        <span class="badge muted">Professional clarity</span>
      </div>

      <div class="grid-3">
        <div class="mini">
          <span class="badge muted">NO</span>
          <h3>합격을 보장하지 않습니다</h3>
          <p>NIW는 사실관계·증거·논증의 종합 결과입니다. 도구는 “정렬”을 돕습니다.</p>
        </div>
        <div class="mini">
          <span class="badge muted">NO</span>
          <h3>RFE를 예측하지 않습니다</h3>
          <p>다만 RFE가 발생하기 쉬운 ‘논리 끊김’ 지점을 사전에 드러내고 보강 방향을 제시합니다.</p>
        </div>
        <div class="mini">
          <span class="badge muted">NO</span>
          <h3>추천서 품질을 자동 평가하지 않습니다</h3>
          <p>추천서는 보조 수단입니다. 핵심은 추천서가 지지할 수 있는 ‘객관 증거 배열’입니다.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- START / CTA -->
  <section class="anchor" id="start">
    <div class="wrap">
      <div class="cta">
        <div class="cta-grid">
          <div>
            <h2>이 프레임에 공감하셨다면,<br/>우리는 같은 언어로 NIW를 논의할 수 있습니다.</h2>
            <p>
              무작위 “가능성 상담”이 아니라, <strong>케이스 구조(Endeavor·증거 배열·논증 흐름)</strong> 중심으로 진행합니다.
              아래는 상담 요청용 간단 폼(데모)입니다. 실제 운영 시 Formspree/서버/CRM에 연결하시면 됩니다.
            </p>
            <div class="divider"></div>
            <div class="muted" style="font-size:13px;">
              ✔ 권장: CV, Google Scholar, 프로젝트 요약(Executive Summary), 대표 산출물 링크를 준비해 주세요.
            </div>
          </div>

          <div class="form" aria-label="Consultation request form">
            <!--
              연결 방법 예시:
              1) Formspree: <form action="https://formspree.io/f/xxxxxx" method="POST">
              2) Cloudflare Worker / API endpoint로 POST
              3) CRM (HubSpot 등) embed
            -->
            <form action="#" method="post" onsubmit="return false;">
              <div class="row">
                <div>
                  <label for="name">성함</label>
                  <input id="name" name="name" type="text" placeholder="예: 홍길동" />
                </div>
                <div>
                  <label for="email">이메일</label>
                  <input id="email" name="email" type="email" placeholder="예: name@example.com" />
                </div>
              </div>

              <label for="field">분야 / Proposed Endeavor(한 문장)</label>
              <input id="field" name="field" type="text" placeholder="예: AI alignment 기반 agent architecture 연구 (연속성 중심으로)" />

              <label for="evidence">보유 증거(간단 체크)</label>
              <div class="choices" style="margin-top:6px">
                <label class="choice"><input type="checkbox" name="evidence" /> 대표 프로젝트 산출물(보고서/논문/특허/제품)</label>
                <label class="choice"><input type="checkbox" name="evidence" /> 독립적 평가(인용, 심사, 초청, 외부 수상 등)</label>
                <label class="choice"><input type="checkbox" name="evidence" /> 미국 맥락 연결 자료(시장/정책/산업 리포트)</label>
              </div>

              <label for="msg">한 가지 질문 (무엇이 가장 고민이신가요?)</label>
              <textarea id="msg" name="msg" placeholder="예: National Importance를 어떤 증거로 설계해야 할지 / EB1A 업그레이드 트랙 가능성 등"></textarea>

              <div class="hero-actions" style="margin-top:12px">
                <button class="btn primary" type="submit">📩 구조화 상담 요청 (데모)</button>
                <a class="btn" href="#sample">샘플 질문 다시 보기</a>
              </div>

              <small>
                * 본 폼은 데모입니다. 실제 운영 시 제출 버튼을 누르면 이메일/CRM로 자동 전송되도록 연결해 드릴 수 있습니다.
                <br/>* “합격 보장” 문구는 사용하지 않습니다. 대신 “구조화/증거 배열” 중심으로 안내합니다.
              </small>
            </form>
          </div>
        </div>
      </div>

      <div class="foot">
        <div class="wrap" style="padding:0">
          <div class="muted">
            © <span id="year"></span> NIW Evidence Mapping Framework. All rights reserved.
          </div>
          <div class="muted" style="margin-top:8px">
            <strong>Disclaimer:</strong> 본 페이지는 일반 정보 제공 목적이며 법률 자문이 아닙니다. 구체 사안은 상담을 통해 검토가 필요합니다.
          </div>
        </div>
      </div>

    </div>
  </section>

  <script>
    // tiny enhancement: current year
    document.getElementById("year").textContent = new Date().getFullYear();

    // Optional: smooth scroll for anchor links
    document.querySelectorAll('a[href^="#"]').forEach(a => {
      a.addEventListener("click", (e) => {
        const id = a.getAttribute("href");
        const el = document.querySelector(id);
        if(!el) return;
        e.preventDefault();
        el.scrollIntoView({behavior:"smooth", block:"start"});
      });
    });
  </script>
</body>
</html>
